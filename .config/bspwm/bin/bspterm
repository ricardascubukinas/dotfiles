#!/usr/bin/env bash

# launch termite with other config
if [ "$1" == "-f" ]; then
	alacritty --config-file /home/o0d4/.config/alacritty/alacritty.yml -e vim
else
	alacritty --config-file /home/o0d4/.config/alacritty/alacritty.yml -e nvim
fi
